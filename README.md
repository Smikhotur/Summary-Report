# 🧾 Summary-Report

> Веб-додаток для автоматизованої обробки Excel файлів для створення зведеної відомості та списка кілометражів для паспортів.

![Preview](https://smikhotur.github.io/Summary-Report/logo.png)

## 🔗 Live Demo

▶️ [Відкрити сайт](https://smikhotur.github.io/Summary-Report/)

## 📦 Основні можливості

- Завантаження одного або кількох Excel-файлів у шаблоні `Картки обліку роботи машин`
- Зчитування **місяця та року з клітинки A5** (наприклад:  
  `На машину Fiat Titano в/н HМ 230 G на листопад місяць 2025 р.`)
- Автоматичний вибір потрібного рядка (комірки) для **«Кілометраж за місяць»**:
  - **лютий → `E42`**
  - **30 днів → `E44`**
  - **31 день → `E45`**
- Пошук номера авто в `A5` з підтримкою форматів:
  - `BM / ВМ ... G / Г`
  - `HM / НМ ... G / Г`
- Формування підсумкового Excel-файлу:
  - **збереження порядку** рядків **у точному порядку вибору файлів**
  - автоматичне створення нового листа `Усі машини`
- Збереження обробленого файлу з новою назвою:
  - `зведений_файл_для_паспортів.xlsx`
  - `зведений_файл_для_зведеної_відомості.xlsx`
  - `ЗВЕДЕНА_ВІДОМІСТЬ_<місяць>.xlsx` (ExcelJS)

## 🧩 Модулі (компоненти)

### 1) ExcelProcessorForPasport (xlsx + file-saver)
Формує таблицю для паспортів:
- `Номер`
- `Кілометраж на початок місяця` (`F6`)
- `Кілометраж за місяць` (`E42/E44/E45` залежно від A5)
- `Кілометраж на кінець місяця` (`F7`)

### 2) ExcelReaderWriter (xlsx + file-saver)
Формує зведену відомість:
- значення беруться з `E/F/G/H/J/M` на кінцевому рядку, який залежить від кількості днів у місяці
- рахує кількість заповнених днів у діапазоні `E15–E43/44`

> ⚠️ Важливо: якщо потрібно працювати **не з “попереднім місяцем від сьогодні”**, а з конкретним місяцем з файлу — використовуй логіку з `A5` (як у `ExcelProcessorForPasport`).

### 3) ExcelMergerExcelJS (ExcelJS + file-saver)
Обʼєднує дані з одного файлу в інший:
- шукає збіг по значенню з колонки `A` (source) у колонці `E` (target)
- переносить значення у колонки `K, G, N, P, R, V, Z`
- якщо значення `0` — очищає клітинку (ставить `null`)
- зберігає файл як: `ЗВЕДЕНА_ВІДОМІСТЬ_<попередній_місяць>.xlsx`

## 🛠️ Технології

- [React](https://reactjs.org/)
- [TypeScript](https://www.typescriptlang.org/)
- [SheetJS (xlsx)](https://github.com/SheetJS/sheetjs) — робота з Excel-файлами в браузері
- [ExcelJS](https://github.com/exceljs/exceljs) — читання/запис Excel + оновлення клітинок
- [file-saver](https://github.com/eligrey/FileSaver.js/) — збереження файлів у браузері
- SCSS-модулі

## 🚀 Як запустити локально

```bash
git clone https://github.com/Smikhotur/Summary-Report.git
cd Summary-Report
npm install
npm start
